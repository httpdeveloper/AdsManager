import {
  StyleSheet,

} from 'react-native';

module.exports = StyleSheet.create({

	defaultStyle: {
		flex: 1,
		alignItems: 'center',
		justifyContent: 'center',
	},

	buttontxt: {
		fontSize: 15,
		margin: 5,
		padding: 5
	},
});
