import {
	StyleSheet,
} from 'react-native';

module.exports = StyleSheet.create({
	spinnercontainer: {
		flex: 1,
		alignItems: 'center',
		justifyContent: 'center',
	},
	spinner: {
		height: 80
	},
});
